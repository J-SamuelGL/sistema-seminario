PISTON_API_KEY=
# Dejar en :80 para servir por IP sin TLS. Si tenés un dominio apuntando
# a la IP del VPS, poné el dominio (p.ej. piston.tu-dominio.com) y Caddy
# obtiene HTTPS automático via Let's Encrypt.
SITE_ADDRESS=:80
